



\documentclass[../Main/main]{subfiles}


\begin{document}


\unit{ $ New $ }
{

	
	
	
	\proposition{ $ Cauchy-Riemman $ }
	{
		\letbe
		{
			\function{ f }{ \R^2 }{ \R^2 } $ satisfies CR $
		}
		\holds
		{
			f_x = f'(z).
			f_y = -if_y.
			\partialderivative{ z }{ f(z) } = 0
		}
		\demonstration
		{
			f_x = u_x + iv_x = a + bi = f'(z).
			f_y = i(a+ib) = if'(z).
			f'(z) = -if_y.
			$notation$
		}
	}


	\example{ $ Conjugation $ }
	{
		\letbe
		{
			\definedFunction{ \bar{a} }{ \C }{ \C }{ z }{ \bar{z} }
		}
		\isNot{ \bar{a} }{ $ holomorphic $ }
		{
			u_x = 1.
			u_y = 0.
			v_x = 0.
			v_y = -1.

			\all{ z \in \C }
			{
				-1 \neq 1 \imp f $ not holomorphic in $ z
			}
		}
	}


	\example{ $ Quadratic norm $ }
	{
		\letbe
		{
			\definedFunction{ f }{ \C }{ \C }{ z }{ |z|^2 }.
			f_{\R^2} $ component decomposition of f $
		}
		\is{ f }{ $ holomorphic in $ 0 }
		{
			f $ differentiable in $ \R^2 $ polinomial $.
			\all{ z \in \C }
			{
				u_x(x,y) = 2x.
				u_y(x,y) = 2y.
				v_x(x,y) = 0.
				v_y(x,y) = 0.

				u_x = v_y \ifandonlyif x = 0.
				u_y = -v_x \ifandonlyif y = 0.

				f $ holomorphic function in $ z \ifandonlyif z = 0
			}
		}
	}
	

	\problem{ $ 3. Cauchy-Riemman $ }
	{
		\letbe
		{
			f \in \Hc( \C ) \suchthat Re f + Im f = c_a
		}
		\showthat
		{
			\ex{ a' \in \C }{ f = c_{a'} }
		}
		\demonstration
		{
			u,v $ real components of $ f.
			u(x,y) + v(x,y) = a.
			$ differentiate respect x and y $.
			u_x + v_x = 0.
			u_y + v_y = 0.
			f $ holomorphic $ \imp f $ CR $.
			u_x - u_y = 0.
			u_y + u_x = 0..

			u_x, u_y, v_x, v_y = 0.
			\ex{ a_1 \in \R }{ u = c_{a_1} }.
			\ex{ a_2 \in \R }{ v = c_{a_2} }.
			f = c_{(a_1,a_2)}
		}
	}


	\problem{ $ B.2 a) $ }
	{
		\letbe
		{
			\definedFunction{ u }{ \R^2 }{ \R^2 }{ (x,y) }{ \exp( y )\cos( x ) }
		}
		\showthat
		{
			\ex{ f \in \Hc(\C) }{ u $ real component of $ f }
		}
		\demonstration
		{
			$lab 1$ \imp u_{xx} + v_{yy} = 0.
			u_x = \exp( x )\cos( y ).
			u_{xx} = \exp( x )\cos( y ).
			u_y = -\exp( x )\sin( y ).
			u_{yy} = - \exp( x )\cos( y ).

			$ok$..

			$ Calculate v using CR $.

			v_y = u_x = \exp( x )\cos( y ).
			v(x,y) = \integral{ \exp( x )\sin( y ) }{ dy }{ \C } = \exp( x )\sin( y ) + \phi(x).

			v_x = \partialderivative{ x }{ v } = \exp( x )\sin( y ) + \phi'(x).
			-u_y = \exp( x )\sin( y ) + \phi'(x).

			$CR$ \imp \phi'(x) = 0.

			\all{ c \in \R }
			{
				\phi(x) = c $ ok $.
			}

			v(x,y) = \exp( x )\sin( y ).

			\definedFunction{ f }{ \C }{ \C }{ (x,y) }{ (u(x,y),v(x,y)) }


		}
	}
	
	
	
	
	
	
	Conformalidad \\

	
	
	
	\proposition{ $ tangent venctor $ }
	{
		\letbe
		{
			\gamma $ differentiable plane arc $ \suchthat \all{ t \in I }
			{
				\gamma'(t) \neq 0
			}
		}
		\holds
		{
			\gamma'(t) $ tangent to $ \gamma
		}
		\demonstration
		{
			demonstration.
		}
	}
	
	
	\proposition{ $ arc images $ }
	{
		\letbe
		{
			f \in \Hc(\Uc).
			\gamma $ differentiable plane arc $ \suchthat \gamma \subset \Uc.
			\sigma = f(\gamma).
			z_0 = \gamma(0)
		}
		\holds
		{
			\sigma' = f'(\gamma)\gamma'.
			\gamma'(0) \neq 0 \imp f'(z_0) \neq 0.
			\sigma'(0) = f'(z_0)\gamma'(0).
			|\sigma'(0)| = |f'(z_0)||\gamma'(0)|.
			arg \sigma'(0) = arg \gamma'(0) + arg f(z_0).
			$ f aplica una homotecia mas una rotacion constante a todos los \\ vectores tangentes que salen de z0 $
		}
		\demonstration
		{
			$obvio$
		}
	}


	
	
	\proposition{ $ Holomorphic functions are conform $ }
	{
		\letbe
		{
			\function{ f }{ \Uc }{ \C }.
			
		}
		\holds
		{
			f $holomorph in $ z \suchthat f'(z) \neq 0 \ifandonlyif f $ conform $
		}
		\demonstration
		{
			\rightway
			{
				$ already seen $
			}
			\leftway
			{
				$ too hard $
			}	
		}
	}
	
	
	\proposition{ $ Preservation of angles $ }
	{
		\letbe
		{
			\gamma_1, \gamma_2 $ plane arcs $ \suchthat \gamma_1(0) = \gamma_2(0)
		}
		\holds
		{
			$ angle of $ \gamma_1'(0) $ and $ \gamma_2'(0) = $ angle $ \sigma_1'(0), \sigma_2'(0) 
		}
		\demonstration
		{
			$ rotations and homotecies let angles invariant $
		}
	}
	
	
	\example{ $ Non preserving angles function $ }
	{
		\letbe
		{
			f(z) = z^2
		}
		\is{ f }{ $ conform in $ \R \nonzero }
		{
			f(\set{ (x,0) \in \C \with x > 0 }) = \set{ (x,0) \in \C \with x > 0 }.
			f(\set{ (x,0) \in \C \with x < 0 }) = \set{ (x,0) \in \C \with x > 0 }.

			ang(A,B) = \pi \neq 0 = ang(f(A),f(B))
		}
	}
	
	

	

	
	
	
	
	
	
	
	
	
	


	
	
	
	
	
	
	



}


\end{document}