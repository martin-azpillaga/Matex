
\documentclass[../Main/main]{subfiles}


\begin{document}


\unit{ $ New $ }
{
	
	\proposition{ $ Homeomorphisms are monotonous $ }
	{
		\letbe
		{
			\function{ f }{ \R }{ \R } $ homeomorphism $
		}
		\holds
		{
			f $ monotonous $
		}
		\demonstration
		{
			$ no demonstration $
		}
	}
	
	
	\proposition{ $ Homeomorphisms and n-periodic points $ }
	{
		\letbe
		{
			\function{ f }{ \R }{ \R } $ homeomorphism $
			( M, T, \phi ) $ dynamical system defined by $ f
		}
		\holds
		{
			\all{ n \in \N }
			{
				\nexists \; x \in M \suchthat x $ n-periodic point $
			}
		}
		\demonstration
		{
			$graphically $
		}
	}
	
	
	\problem{ $ Decreasing function orbits $ }
	{
		\letbe
		{
			declarations.
		}
		\showthat
		{
			statements.
		}
		\demonstration
		{
			f $ corta en un punto $.

			f $ decreasing $ \imp f^2 $ increasing $.

			f^{2n} \convergesto $ fixed point of $ f
		}
	}
	
	
	\problem{ $ 10. $ }
	{
		\letbe
		{
			\function{ f }{ \R^+ }{ \R^+ } \in \C^{\infty}.
			f(0) = 0.
			p \in \R^+ \nonzero \suchthat f'(p) \geq 0.
			f' $ decreasing $
		}
		\showthat
		{
			\all{ p \in \R^+ \nonzero }
			{
				f^n(x) \convergesto p
			}
		}
		\demonstration
		{
			f' $ decreasing $ \imp f'' < 0 \imp f $ concave $.

			f $ positive $ \imp f $ has no extrema $ \imp f' > 0 \imp f $ increasing $.


			f $ has only one fixed point $.

			$Suppose 2 fixed points $: p,p'.

			IVT \imp \ex{ c \in (0,p') }
			{
				f'(c) = 1
			}.

			f'(p) < 1 \imp p $ attractive $

			IVT \imp $ dont exist more fixed points $ \imp f'(c') = 1 \nleq 1.

			\all{ x \in (0,p) }
			{
				f(x) > x
			}.

			\all{ x \in \R }[ x > p ]
			{
				f(x) < x
			}.

			f $ increasing $ \imp f([0,p]) = [0,p].
			f $ increasing $ \imp f([p,\infty)) \subset [p,\infty).

			$ convergence tests $ \imp $OK$
		}
	}
	
	
	\definition{ $ Logistic $ }
	{
		\letbe
		{
			\function{ f }{ \R }{ \R }.
			( M, T, \phi ) $ dynamical system defined by $ f
		}
		\then{ ( M, T, \phi ) }{ $ a logistic dynamical system $ }
		{
			\ex{ a \in \R }
			{
				\definedFunction{ f }{ \R }{ \R }{ x }{ ax(1-x) }
			}
		}
	}
	
	
	\proposition{ $ Analysis of logistic dynamical systems $ }
	{
		\letbe
		{
			( M, T, \phi ) $ logistical dynamical system defined by $ f
		}
		\holds
		{
			Fix(f) = \set{ 0,\frac{ a-1 }{ a } }.
			Per_2(f) = 
		}
		\demonstration
		{
			demonstration.
		}
	}
	
	
	\definition{ $ Chaos $ }
	{
		\letbe
		{
			\function{ f }{ \R }{ \R }.
			( M, T, \phi ) $ dynamical system defined by $ f
		}
		\then{ ( M, T, \phi ) }{ $ chaotic $ }
		{
			Fix(f) $ dense in $ \R.
			\ex{ x \in \R }
			{
				o(x) $ dense in $ \R
			}.

			f $ sensibility of $ x_0
		}
	}
	
	
	\definition{ $ Sarkovskii's order $ }
	{
		\name{ $ Sarkovskii's order	$ }
		{
			naming.
		}
		\denote
		{
			property \as notation.
		}
	}
	
	
	
	\proposition{ $ Sarkovskii's theorem $ }
	{
		\letbe
		{
			\function{ f }{ I }{ I }.
			( M, T, \phi ) $ dynamical system $
		}
		\holds
		{
			\ex{ x \in M }
			{
				o(x) $ k-period $
			}.

			\imp \all{ l \in \N }[ l > k ]
			{
				\ex{ x' \in M }
				{
					x' $ l-period $
				}
			}
		}
	}
	
	
	
		

}


\end{document}